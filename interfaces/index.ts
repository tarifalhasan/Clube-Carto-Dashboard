export interface ButtonGroupType {
  name: string;
  href: string;
}

export interface AccociadorListType {
  name: string;
  id: string;
}
export interface DataTableType{
  data:string,
  protocolo: string,
  descrição:string,
  valor: string,
  ações:string
}